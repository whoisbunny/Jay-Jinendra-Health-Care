
export const metadata = {
    title: "Our Health Care Services",
    description: "Explore the range of health care services we offer to support your well-being.",
    keywords: ["health care", "services", "well-being", "medical support"],
    robots: "index, follow",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
